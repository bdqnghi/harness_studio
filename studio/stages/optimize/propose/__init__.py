"""Propose phase: the hypothesis tree and what grows on it. ``idea_tree`` is the
durable memory (directions → hypotheses); ``ideator`` generates the next
hypothesis for a direction; ``insight`` distills a lesson from each rejection so
dead ideas are never re-bought."""
